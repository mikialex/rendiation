[workspace]
members = [
    "math",
    "util",
    "math-entity",
    "render-entity",
    "rendiation",
    "render-graph",
    "rendium",
    "rinecraft",
]

[profile.release]
# Tell `rustc` to optimize for small code size.
# opt-level = 's'

# opt-level = 3