// use rendiation_lighting_ibl_gpu::*;
// use rendiation_texture::*;
// use rendiation_webgpu::*;

pub fn main() {
  //
}

// pub async fn generate_lut() {
//   let (gpu, _) = GPU::new(Default::default()).await.unwrap();

//   let attachments = AttachmentPool::default();
//   let mut frame = FrameCtx::new(&gpu, Size::from_u32_pair_min_one((1, 1)), &attachments);

//   let target = todo!();

//   generate_brdf_lut(&mut frame, target);
// }
