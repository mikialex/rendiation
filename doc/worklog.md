
# 工作日志

- 2026-08-27 09:52 「代码研究和文档编写」完成 GPU 光照系统:输出 doc/content/lighting-system-guide.md(426 行)。研究结论:LightSystem(常驻配置,Viewer3dRenderingCtx 创建)→use_lighting(Update/CreateRender 两阶段产出 LightingRenderingCxPrepareCtx)→LightSystem::prepare(帧内画全部阴影贴图+组装 SceneLightSystem)→use_render_lighting_scene_content(每视口上屏);四层光照计算 trait(LightSystemSceneProvider→LightingComputeComponent→LightingComputeInvocation→LightableSurfaceShading)+阴影侧 AbstractShadowMapGPUData/AbstractShadowComputer 等,10+ 下游实现;LightableSurfaceTag/ColorChannel/HDRLightResult 接线链与 ForwardLightingEmissiveAdd 单次绘制约束;MultiLayerTexturePackerRaw+RemappedGrowablePacker 阴影图集(2D 尺寸翻倍优先/到头增层/到 max_size 失败),每光源类型一份 2048³,ShadowMapAddressInfo 与灯光 uniform 数组同下标对齐;五类光源 preparer(方向 Basic/Cascade/NoShadow 三态、聚光、点光 cube 2×3 面、区域光 LTC LUT+downcast_ref 门控、IBL 预过滤立方图+BRDF LUT);Forward dispatcher 合成/DeferLighting g-buffer+FrameGeneralMaterialBuffer 编解码(Pbr/Unlit/Phong 三套)/透明对象单独 forward;PerSceneLightUniformArray 两后端共用,LIGHT_LIST_LEN=8,差分上传 upload_with_diff。重要逻辑问题:PhongSurfaceEncodeDecode::decode(defer_protocol.rs:377) emissive 取错通道(取了 alpha/specular.r/specular.g,应为 channel_b.w/channel_c.x/channel_c.y,对照 Pbr 实现 :299-303),occ 风格材质 DeferLighting 下 emissive 完全错误且 alpha 被复用为 emissive 红通道。建议下一个任务:texture-system-guide(已在计划,中优先级)
- 2026-08-27 09:49 「代码研究和文档编写」完成 webgpu 缓冲资源层:输出 doc/content/webgpu-buffer-layer-guide.md(372 行)。研究结论:AbstractBuffer(全部字节单位,resize 与 relocation 合一全量拷贝、同缓冲批量搬迁临时缓冲防重叠、get_gpu_buffer_view 返回 None 为纹理模拟路径判据);AbstractStorageAllocator 可插拔三实现(DefaultStorageAllocator 真 GPU buffer/TextureAsStorageAllocator GLES-only R32Uint 纹理模拟+宿主备份+伪指针 hack/CombinedStorageBufferAllocator 实验性);linear_buffer_array 六能力 trait+五组合子(with_direct_resize/with_queue_direct_update/with_vec_backup/with_default_value_with_init_write/with_default_grow_behavior),组合顺序有约束(SparseUpdateStorageBuffer 刻意不挂直写组合子);slab(GPUSlatAllocateMaintainer 槽位复用)与 range(GPURangeAllocateMaintainer,GrowableRangeAllocator+offset→id 反查表+RelocationMessage 回调)两种槽位语义;下游 SBT 三池+字段级更新、SceneModelListPool 不用 grow 的原因、task-graph allocate_dyn_ty 等。重要逻辑问题:GPUSlatAllocateMaintainer::deallocate_back(slab.rs:38) 递归调用自身无终止条件,任何调用栈溢出,被 SBT 释放路径真实调用(sbt.rs:201)触发即崩溃;另 used_count 从未更新 current_used 恒 0 属隐患。建议下一个任务:ray-tracing-guide(SBT 是最大非 hook 消费者)
- 2026-08-27 09:43 「文档整合检查」第三批完成:检查 6 份新 guide(viewer-content-frame-pipeline/webgpu-hook-utils/gles-material-host-render/extension-indirect-render-patterns/task-graph/attribute-mesh-lod,均移出「新文档编写未检查区」)。冲突修复 3 处:attribute-mesh-lod-guide 的 level_meta.z 与基座 AttributeMeshMeta.count 语义对齐(基座以 u32 槽计、host 访问 ×2 展开,原文与 attribute-mesh-indirect-render-guide 矛盾,已修正并双向引用);occlusion-culling-guide(已检查)OC 状态键控修正(源码证实每相机一份,原文"(相机, 视口)"错误,viewer guide 本就正确未动);gles-material-host-render-guide 纹理选型补 RTX 例外(frame_all.rs:123 is_indirect || rtx_renderer_enabled)。重复归并 3 处:viewer guide Gles 分支压缩为帧组织视角+引用 gles guide、gles guide 后端选择删分流细节改引用 viewer guide、material guide GLES 对照节补展开链接。过期引用修复 3 处(「计划中」改真实链接、light_pass 锚点 :24→:20、LOD 简述改 LODConversionMode 准确描述)。未拆分(extension-patterns 511 行保留整体,四扩展对照结构有先例)。未发现新重要逻辑问题(task.md 保留 occ-style TopMost 位与 bug)。新增建议:texture-system-guide(中优先级,content/texture/gpu-system 三实现分散两处无专文)、viewer 透明渲染(低优先级)
- 2026-08-27 09:32 「代码研究和文档编写」完成 attribute-mesh LOD:输出 doc/content/attribute-mesh-lod-guide.md(336 行)。研究结论:两级抽象分离——转换(process_attribute_mesh_lod,数据变化时 CPU 并行简化:QEM 边折叠+sloppy 顶点聚类回退,HalfCount/ErrorDoubling 两模式)与选择(渲染时 GPU 逐实体逐帧按投影误差选级);合并索引缓冲保证 origin 在前非 LOD 消费者无感;lod_error_threshold 像素阈值经世界缩放(矩阵最大列长)与投影矩阵 z 列 w 分量区分透视/正交投影成屏幕像素,从粗到细选首个 ≤ 阈值级;device 侧逐实体选级/host 侧只画根级(level_meta.z)/GLES 完全不经过 LOD/阴影 pass 同相机同阈值;view_dependent_transform 经 NodeGPUStorageWithOverride 覆盖节点访问器(世界缩放来自覆盖矩阵,保守距离仍用场景图包围盒);level_meta Vec4 每 mesh 一条+lod_levels 每级一条,u16 每级尾部补齐偶数保证原生/MIDC 降级 base index 可直接相加;trait 包装式扩展实现 IndirectDrawProviderCreator/DrawCommandBuilderCreator/IndirectModelShapeRenderImpl。无重要逻辑问题。建议下一个任务:content/mesh/simplification 网格简化 crate(QEM quadric 累加/顶点分类/border loop、sloppy 聚类尺寸二分+插值搜索、SimplificationResult 误差换算,当前无任何覆盖)
- 2026-08-27 09:32 「代码研究和文档编写」完成 GPU 任务图运行时:输出 doc/content/task-graph-guide.md(326 行)。研究结论:rendiation-device-task-graph 约 2600 行,任务组/任务池/状态机(状态 0~4:不存在/唤醒待轮询/已轮询待压缩/休眠/已完成,父子边经 wake_task_dyn GPU 侧推进);宿主 execute(round_count) 定点驱动,每轮 per-group compute pass 间接派发,轮末 parallel-compute 流压缩剔除休眠任务(ActiveTaskCompact),槽位 0 默认任务保证越界线程 uniform 控制流;bump_allocator 两阶段分配(线程内 atomic bump+派发间 commit_size 折叠);future 为 ShaderFuture(构建)+ShaderFutureInvocation(轮询)双 trait 与 map/then/then_spawn_task 组合子;dyn_ty_builder 动态状态结构体指针延迟解析;唯一消费者 wavefront 光线追踪。无重大逻辑问题(wake_task_dyn 重复唤醒同轮活跃任务属结构性缺口,当前组合子与消费者均不触发,已以「一轮一个活跃子任务」警示写入文档)。建议下一个任务:shader/ray-tracing 光线追踪 API 层与 wavefront 后端(TLAS 双实现/NSBT/trace_task,约 3000+ 行无文档覆盖)
- 2026-08-27 09:32 「代码研究和文档编写」完成 GLES host 渲染路径:输出 doc/content/gles-material-host-render-guide.md(283 行,130 个链接已验证)。研究结论:gpu-gles 把「一个场景模型怎么画」切成四层对象安全 trait(SceneModelRenderer/GLESModelRenderImpl/GLESModelMaterialRenderImpl 等),全实现 Vec 首匹配语义,下游 13 个渲染器;材质路径为 UniformBufferCollection 每材质实体 std140 uniform 增量写字段偏移,替代存储数组;setup_tex 双句柄设计(host pair 现场解析真实 GPU 资源直绑/device pair 仅用于 u32::MAX 哨兵判断),同一 sample_texture2d_with_shader_bind 接口对直绑/纹理池/bindless 通用;use_gles_scene_model_renderer 逐模型 7 组件 RenderArray 发 TraditionalDraw;indirect 路径未接通蒙皮(骨矩阵纹理渲染为 GLES 独有)。无新增重要逻辑问题(独立复现 occ-style-draw-control TopMost 位与 bug 已被前任务记录;sg.rs glossiness_texture 死字段属观察)。建议下一个任务:content/lighting GPU 光照系统(第三次被独立推荐,最大引用空缺)
- 2026-08-27 09:31 「代码研究和文档编写」完成 extension 间接绘制模式综述:输出 doc/content/extension-indirect-render-patterns-guide.md(511 行)。研究结论:wide-line/wide-styled-points/text-3d/cell-mesh 四扩展共享「payload 外键+注册函数」数据模型、「几何池+参数行+索引映射」三段式渲染器、「host 现场算/compute 生成」双表达 NoneIndexedDrawCommandBuilder(分配失败 count 恒 0)+ ForeignHash 增量 key + LocalModelPicker 三件套;关键分岔是前三个为独立 scene model 类型(IndirectModelRenderImpl/model 槽)而 cell-mesh 是标准模型形状(IndirectModelShapeRenderImpl/mesh 槽);特有差异:宽线 18 顶点 3 行 sprite+1px LineList 优化、宽点 6 顶点+16 种 SDF 样式+NDC 像素膨胀拾取、text-3d slug 曲线/band 三池、cell-mesh 双面颜色+shrink_ratio。无重要逻辑问题(cell-mesh 无 GLES 实现属设计空缺,GLES 下不可渲染但 CPU 拾取可用)。建议下一个任务:extension/transform-instanced-model 实例化模型扩展(同模式最复杂变体:实例池+跨扩展顶点预算+primitive_index 覆写,可顺带覆盖 view-dependent-transform)
- 2026-08-27 09:29 「代码研究和文档编写」完成 webgpu-hook-utils:输出 doc/content/webgpu-hook-utils-guide.md(392 行)。研究结论:26 个下游 crate 依赖的渲染侧公共底座;QueryGPUHookCx 将 hook 运行时与增量查询特化为 Update(spawn:收集变化/worker 打包/task pool 安装)/CreateRender(resolve:按 token 取回/帧 encoder 写 GPU/when_render 组装渲染器)两阶段,同一 hook 函数每帧执行两遍靠 stage 区分,驱动循环在 rendering_root.rs::draw_canvas;trait 体系为 DataChangeGPUExt(同步 DataChanges)与 DataChangeGPUExtForUseResult(异步 UseResult)统一入口,三条通道(uniform 集合/uniform 数组/稀疏存储数组);稀疏写经 SparseBufferWritesSource 打包(数据+u32 三元组偏移表)render 阶段一次 compute 派发落地;GrowableRangeAllocator(xalloc TLSF)产出四类互斥变更经 BatchAllocateResultShared 合并 relocation 进 resize。无重要逻辑问题(MultiAccessGPUIter 下溢与槽位不复用清零均属隐患/约定)。建议下一个任务:platform/graphics/webgpu 缓冲资源层(AbstractBuffer/AbstractStorageAllocator/linear_buffer_array 组合子,是本文与 batch-extractor 的直接上游地基,无文档覆盖)
- 2026-08-27 09:29 「代码研究和文档编写」完成 viewer 帧流水线:输出 doc/content/viewer-content-frame-pipeline-guide.md(316 行)。研究结论:RenderingRoot/Viewer3dRenderingCtx/ViewerDataScheduler 装配与 surface 生命周期账本、draw_canvas 五段流程(init_frame→FrameCtx→按需渲染检查→Update 增量维护→CreateRender 组装→逐视口 keyed_scope 编码)、use_viewer_scene_renderer 处 device/Gles 双后端分流、frustum 剔除在此文首次完整覆盖(device 批 use_culled_list_and_do_culling/host 批 HostFrustumCulling 迭代器)、OC 装配与 will_collecting/feedback 帧序、LightSystem 阴影图集与 Forward/DeferLighting 接入、逐视口 pass 序列。重要逻辑问题:occ-style-draw-control/src/gles.rs:42 get_top_most_layer 排序 key 写为 layer & priority(位与)应即位或,TopMost 层 priority 排序失效。建议下一个任务:光照系统 lighting-system-guide(application/viewer-content/src/rendering/lighting/,阴影图集打包/五类光源 preparer/延迟材质注册表/tonemap,目前无任何文档)
- 2026-08-27 09:21 「文档整合检查」第二批完成:检查 material-indirect-render-guide / parallel-compute-primitives-guide / gpu-indirect-batch-collector-guide(均移出「新文档编写未检查区」)。修复 1 处逻辑冲突(batch-extractor-guide GLES 变体节:indirect_batch_direct_creator 在非 host-driven 模式提供、host-driven 模式返回 None,原文档括号写反,已按 scene.rs:159 与 batch_extraction.rs:100 核实修正);收敛 4 处重复(parallel-compute 两阶段扫描算法细节从 draw-list-guide 精简为引用、use_make_scene_batch_pass_content 组织链路从 draw-list-guide/indirect-draw-command-guide 精简为引用 batch-collector guide、material guide 删除与 batch-extractor guide 重复的 hash_shader_group_key 代码片段、attribute-mesh guide 帧内消费流程改指 batch-collector guide);修正 6 处代码链接行号(material guide: std_model.rs:484→487、scene_model.rs:95→96、pool.rs:366→367、bindless.rs:29→30;batch-collector guide: scene.rs:421→422、culling.rs:67→66)。全部算法与机制断言已逐条对照代码核实(段前缀和规模断言注释、Kogge-Stone 双屏障循环、流压缩 size pass、材质 PSO 哈希仅 alpha_mode、use_make_scene_batch_pass_content 三分支、RenderBatchCollector 帧序等),未发现源代码明显逻辑错误。未拆分/聚类文档。更新 doc/index.md 摘要(draw-list 与 parallel-compute 交叉引用提示)。建议后续任务:见下一条 webgpu-hook-utils 建议,另可优先执行 viewer-content-frame-pipeline-guide 与 attribute-mesh-lod-guide(均为本次检查中被多文档引用的空白)
- 2026-08-27 00:41 「代码研究和文档编写」完成 gpu-indirect 批收集链路:输出 doc/content/gpu-indirect-batch-collector-guide.md(262 行)。研究结论:RenderBatchCollector 目前仅 DoNothingRenderBatchCollector 一个实现,唯一真实消费方是遮挡剔除 feedback_culling_result(will_collecting 帧前控制 OC 保留剔除结果、is_collecting 帧末反馈+flush),注入点在 ViewerDataScheduler.batch_collector;拾取走 BVH 光线查询与渲染批解耦。无重大依赖。建议下一个任务:attribute-mesh-lod LOD 系统
- 2026-08-27 00:40 「代码研究和文档编写」完成 shader/parallel-compute:输出 doc/content/parallel-compute-primitives-guide.md(269 行)。研究结论:段前缀和输出是全局 inclusive 扫描("segmented" 指两阶段分块而非经典段标志扫描);规模上限约束 first_stage × second_stage,断言被注释超限不报错。已核对 draw-list-guide 与 indirect-draw-command-guide 提及处无出入。无重大依赖。建议下一个任务:shader/task-graph GPU 任务图运行时
- 2026-08-27 00:39 「代码研究和文档编写」完成 gpu-indirect/material:输出 doc/content/material-indirect-render-guide.md(251 行)。研究结论:三类标准材质以「每材质实体一个 GPU 存储槽位」投影为参数与纹理句柄双缓冲,材质 id 经 SceneStdModelStorage 元数据表在顶点阶段注入,纹理经 GPUTextureBindingSystem(纹理池手动采样/bindless)间接采样,alpha_mode 同时驱动 PSO 哈希与批提取分组 key。无重大依赖。建议下一个任务:gpu-gles 材质与 host 渲染路径
- 2026-08-27 00:35 「文档整合检查」完成:修复 occlusion-culling-guide 分层动机节自相矛盾(上帧可见批同样每帧跑金字塔测试,仅结果是否用于本帧剔除不同,已按代码核实);收敛 4 处重复(MIDC 降级机制归并至 indirect-draw-command-guide,attribute-mesh guide 精简为属性网格特有细节并补 viewer 侧 force 开关两来源;AttributeMeshIndirectDrawCreator 实现示例压缩为模式级描述;帧内消费流程改为引用 mid guide;TopMost 渲染交叉引用 batch-extractor/viewer-content-api)。未新建/拆分/聚类文档。「新文档编写未检查区」已清空。补充建议:webgpu-hook-utils 渲染侧 hook 基建、viewer-content 帧流水线、extension 间接绘制实现模式综述
- 2026-08-27 00:30 「代码研究和文档编写」完成 occlusion-culling:输出 doc/content/occlusion-culling-guide.md(272 行)。研究结论:GPUTwoPassOcclusionCulling 按 (相机, 视口) keyed scope 每相机一份,last_frame_visibility 存「不可见」标志(1=不可见),零初始化默认态为可见,首帧全量进 occluder 遍、第二帧起收敛;唯一消费者为 application/viewer-content 的 use_viewer_culling。无重大依赖。建议下一个任务:gpu-indirect 间接绘制/批收集链路
- 2026-08-27 00:26 「代码研究和文档编写」完成 gpu-indirect/shape/attribute:输出 doc/content/attribute-mesh-indirect-render-guide.md(205 行)。研究结论:AttributeMeshMeta 定义于 gpu-indirect/src/shape/attribute/mod.rs:498;宿主侧与 compute 侧双通道共享同一元数据表;u16 索引 count 语义为 u32 槽位数;used_in_midc_downgrade 影响索引基址单位并进入管线哈希。无重大依赖。建议下一个任务:gpu-indirect/material 材质侧文档
- 2026-08-26 23:44 「代码研究和文档编写」完成 gpu-base/src/mid:输出 doc/content/indirect-draw-command-guide.md(274 行,26 个代码链接已验证)。研究结论:Indexed/NoneIndexed 双分支 builder trait + generator 组件 + provider 三合一;webgpu-midc-downgrade 段前缀和降级机制完整覆盖。无重大依赖。建议下一个任务:shader/parallel-compute 流压缩与段前缀和文档
